# ZETUSHIN Development Log

## Research Layer Expansion (Pre-Final State)

| Date | Purpose | Reason | Implementation | Commit ID | Affected Modules |
|------|---------|--------|----------------|-----------|------------------|
| 2026-03-06 09:40 | Eliminate the risk of developers enabling unreleased testing layers in the production environment by using `window.localStorage`. Ensure automated prevention of DEV UI leakage. | The system constitution explicitly forbids medical claims and developer dashboards appearing on the production (client-facing) app. Hardening the application against accidental local toggling removes a severe security vulnerability. | Added a Playwright E2E suite (`tests/e2e/auditDOM.spec.ts`) testing the live DOM. Added execution to `scripts/audit/run.sh` to enforce completion. Removed `window.localStorage` usage entirely in `utils/featureFlags.ts`. Substituted testing enabling logic with `VITE_DEV_FEATURE_OVERRIDES` environment variables, retaining `IS_PROD` absolute hardware locks. | `c5d4f1f` | `utils/featureFlags.ts`<br>`tests/featureFlags.test.ts`<br>`tests/e2e/auditDOM.spec.ts`<br>`scripts/audit/run.sh` |
| 2026-03-06 09:33 | Add an overarching orchestration layer to manage the full research lifecycle and prepare for dummy image injection testing. | Needed a structured path predicting data shortage, identifying target progression towards stable prediction models, and producing logical output without involving live user data prematurely. | Centralized `ResearchStage` definitions (`DATA_COLLECTION` to `MODEL_REFINEMENT`) inside `constants/researchStages.ts`. Added `services/research/researchOS.ts` linking metrics, data gaps, and model readiness `calculateModelReadiness()` to determine completion thresholds. Connected `ResearchPlanner` to issue recommended instructions tracking missing variables. Developed `scripts/research_seed_stub.mjs` for dummy data and `docs/image_injection_playbook.md` playbook. | `51567d5`, `375c00c` | `services/research/researchOS.ts`<br>`services/research/researchPlanner.ts`<br>`services/research/modelReadiness.ts`<br>`components/ResearchDashboard.tsx`<br>`constants/researchStages.ts` |
| 2026-03-06 08:58 | Calculate pure metric features natively (e.g., Redness Index), classify entries based on a specific heat-cold framework, and calculate an expert consistency score. | The raw DB requires structuring into standardized arrays before training ML models on them. It acts as an integration step converting image characteristics into structured metrics. | Evaluator Engine: Built `services/research/expertEvaluation.ts` providing agreement rate, exclusion explanations, and distribution matrices comparing Phase 1 model outputs versus Expert Observations. Vision Proxy Extraction: Built `services/vision/featureExtractorV1.ts` returning completely neutral, purely calculated null-safed optical characteristics. Temperature Estimation Engine: Extracted logic to `services/ai/heatColdEstimatorV0.ts` with explicit restrictions overriding deterministic findings if inputs conflict (`HOLD`). Quality Scaling: Designed `services/research/qualityScore.ts` penalizing bad data using custom scaling weights. | `74e7313` | `services/research/expertEvaluation.ts`<br>`constants/heatColdSpectrum.ts`<br>`constants/researchQualityWeights.ts`<br>`services/vision/featureExtractorV1.ts`<br>`services/ai/heatColdEstimatorV0.ts`<br>`services/research/qualityScore.ts`<br>`tests/newResearchModules.test.ts`<br>`utils/featureFlags.ts` |

